

const fetchNews = async (
    category?: Category | string,
    keywords?: string,
    isDynamic?: boolean
) => {
    // graphQL query

    // fetch function with NextJS 13 caching

    // sort function so that articles with images displayed at top

    // return result
}


export default fetchNews;